/* Services Gallery - Gallery Cards Pattern (TRD Branded) */

.services-gallery {
  position: relative;
  width: 100vw;
  height: 100svh;
  overflow: hidden;
  background-color: #f5f5f5; /* Light background */
  color: #1a1a1a; /* Dark text */
}

/* Header */
.services-gallery-header {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 40%;
  text-align: center;
  z-index: 1;
}

.services-gallery-header h3 {
  font-family: "Rader", sans-serif;
  font-size: 4rem;
  font-weight: 700;
  letter-spacing: -0.15rem;
  line-height: 1.125;
  color: #1a1a1a;
}

/* Decorative Bars */
.services-top-bar,
.services-bottom-bar {
  position: absolute;
  width: 100%;
  padding: 0.5rem 0;
  background-color: transparent;
  border-top: 1px solid rgba(26, 26, 26, 0.1);
  border-bottom: 1px solid rgba(26, 26, 26, 0.1);
  z-index: 1;
}

.services-top-bar {
  top: 2rem;
}

.services-bottom-bar {
  top: 5rem;
}

.services-top-bar .container,
.services-bottom-bar .container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 2rem;
}

.symbols-container {
  display: flex;
  gap: 0.5rem;
}

.symbol {
  width: 8px;
  height: 8px;
  background-color: #1a1a1a;
  border-radius: 50%;
}

.mono {
  font-family: "Messina Sans", monospace;
  font-size: 0.75rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.mono span {
  margin-right: 0.25rem;
}

/* Gallery Cards */
.gallery-card {
  position: absolute;
  top: 25%;
  left: 50%;
  transform: translate(-50%, -50%);
  will-change: transform;
  width: 25%;
  height: 50%;
  padding: 0.5em;
  display: flex;
  flex-direction: column;
  gap: 0.5em;
  border-radius: 12px;
  background-color: #1a1a1a; /* Dark card background */
  color: #f5f5f5; /* Light text */
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
  z-index: 2;
}

.gallery-card-img {
  flex: 1 1 0;
  min-height: 0;
  width: 100%;
  border-radius: 8px;
  overflow: hidden;
}

.gallery-card-img img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

.gallery-card-content {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  padding: 0.25rem 0.5rem;
}

.gallery-card-content p.mono {
  font-size: 0.7rem;
  color: #f5f5f5;
}

/* Responsive Styles */
@media (max-width: 1000px) {
  .services-gallery-header {
    width: 90%;
  }

  .services-gallery-header h3 {
    font-size: 2rem;
    letter-spacing: -0.05rem;
  }

  .services-top-bar,
  .services-bottom-bar {
    padding: 0.25rem 0;
  }

  .services-top-bar .container,
  .services-bottom-bar .container {
    padding: 0 1rem;
  }

  .mono {
    font-size: 0.6rem;
  }

  .gallery-card {
    width: 75%;
    height: 40%;
    top: 50%;
  }

  .gallery-card-content p.mono {
    font-size: 0.6rem;
  }
}

@media (max-width: 640px) {
  .gallery-card {
    width: 85%;
    height: 35%;
  }

  .services-gallery-header h3 {
    font-size: 1.5rem;
  }
}
